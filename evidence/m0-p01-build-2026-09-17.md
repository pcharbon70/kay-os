# M0 Phase 1 Section 1.2 execution record

## Scope

- Task: `m0-p01-build`
- Date: 2026-09-17
- Repository branch: `codex/m0-phase-01-baseline`
- Tested revision: `00f5c31` (`M0 phase 1 section 1.2: qualify freestanding build`)
- Tested state: clean worktree
- Record revision: the Section 1.2 commit was amended only to replace the
  earlier dirty-tree observation with this clean-revision result; executable
  inputs are identical to tested revision `00f5c31`
- Environment: Linux x86-64 host; no QEMU guest and no physical T7500

## Inputs

- Zig: `0.16.0`
- Zig executable SHA-256:
  `2317bbb91798556d9d0f38aabdac23db83f0979b25f767259ae474546724087c`
- Backend/linker: LLVM/LLD, with compiler-rt and UBSan runtime disabled
- Target/CPU: `x86_64-freestanding-none`, `nehalem`
- Driver: `scripts/m0/verify-build-closure.sh`

## Invocation

```sh
ZIG=/home/ducky/.asdf/installs/zig/0.16.0/zig \
  scripts/m0/verify-build-closure.sh \
  /tmp/kay-m0-section-1-2-evidence-v4
```

The driver created two clean source copies below a private temporary directory,
used distinct absolute paths and caches, built the stripped and audit fixtures,
compared outputs, audited the ELF and disassembly, and ran all three negative
cases. The private build directories were removed when the driver exited.

## Actual results

- Overall result: pass
- Acceptance artifact SHA-256 from both builds:
  `e94b127fcc4388e73b4620a8b0908dcdeed0c3a08fbf0052d92cce58a3baf31a`
- Acceptance artifact comparison: byte-for-byte equal
- Audit symbol-map comparison: equal
- Audit ELF-map comparison: equal
- ELF audit: ELF64, static `ET_EXEC`, x86-64, entry `0x200000`, no
  `INTERP`/`DYNAMIC`, no unresolved audit symbols
- FP/SIMD audit: no x87, MMX, XMM, YMM, or ZMM use observed
- Required native symbols observed: `_start`, `kay_fixture_main`,
  `kay_c_accumulate`, `kay_zig_callback`, `kay_panic`, `__stack_bottom`, and
  `__stack_top`

Observed negative-link output included:

```text
error: ld.lld: undefined symbol: __udivti3
error: ld.lld: undefined symbol: write
error: invalid option: -Dcpu
```

Each command returned nonzero and the driver matched the expected diagnostic.
An unexpected successful link would have failed the run.

## Permitted difference and limitations

The two audit ELF hashes differed because their DWARF encoded their distinct
absolute source paths. This audit-only difference is permitted; the directly
generated Zig `-fstrip` acceptance ELFs and normalized maps matched. No guest
execution occurred. This record does not qualify boot, firmware handoff,
physical hardware, SMP, interrupt state, or the future kernel ABI.

Section 1.2 is supported by this result. Section 1.1 remains open because the
physical inventory was deferred, and Section 1.3 plus phase handoff remain
unrun and unreviewed.
